# Import secrets
spring.config.import=optional:secrets.properties

# Database Configuration
spring.datasource.url=jdbc:postgresql://aws-0-us-east-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.dwinwucynynxwhobsyqt
spring.datasource.password=Dhrumit@246
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.jpa.properties.hibernate.schema_validation.enabled=false

# TypeDef mappings for Hibernate types
spring.jpa.properties.hibernate.types.print.banner=false
spring.jpa.properties.hibernate.type.json_format_mapper=jackson

# OpenAI Configuration
openai.api.key=${OPENAI_API_KEY}

# Twilio Configuration
twilio.account.sid=${TWILIO_ACCOUNT_SID}
twilio.auth.token=${TWILIO_AUTH_TOKEN}
twilio.phone.number=${TWILIO_PHONE_NUMBER}

# Stripe Configuration
stripe.api.key=${STRIPE_API_KEY}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET}

# JWT Configuration
jwt.secret=${JWT_SECRET:defaultsecretkey}
jwt.expiration=86400000
 
# Admin Configuration
admin.default.email=${ADMIN_EMAIL:admin@aifriend.com}
admin.default.password=${ADMIN_PASSWORD:admin123}
admin.default.firstName=${ADMIN_FIRST_NAME:Admin}
admin.default.lastName=${ADMIN_LAST_NAME:User}

# Server Configuration
server.port=8090
